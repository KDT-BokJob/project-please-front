import { Button } from '@/components/ui/button'
import { Camera, ChevronLeft, DefaultProfile } from '@/lib/icons'
import { cn } from '@/lib/utils'

export default function page() {
  return <h1>resume page</h1>
}
